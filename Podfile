# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SerialKeyGenerator' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
	pod 'CryptoSwift'
  use_frameworks!

  # Pods for SerialKeyGenerator

  target 'SerialKeyGeneratorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SerialKeyGeneratorUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
